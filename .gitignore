# 上游 Codex 克隆，单独管理，不纳入本仓库
codex-upstream/

# 上游 Hermes Agent 克隆（WORK 引擎黑盒），同 codex 待遇，不纳入本仓库
hermes-upstream/

# 依赖与构建产物
node_modules/
target/
dist/
.DS_Store

# 密钥（绝不入库）
.env
.env.*.local
*.key
**/secrets.*

# 本地验证用的隔离环境（不入库）
.scratch/

# Python
__pycache__/
*.pyc
