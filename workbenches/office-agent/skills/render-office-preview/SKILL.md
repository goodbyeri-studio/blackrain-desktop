# Render Office Preview

把 Office 文件渲染成可检查的预览结果。

## 适用任务

- 生成文档后检查结果
- 修改版式后确认修复是否生效
- 需要给用户展示最终排版效果

## 默认流程

1. 判断预览目标是 HTML 还是 PNG
2. 调用内置 `officecli view ...`
3. 检查输出
4. 如发现明显问题,返回修正文档步骤

## 原则

- 渲染预览是办公智能体的重要闭环,不是可有可无的附加步骤
