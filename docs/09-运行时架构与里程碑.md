# 09 运行时架构与里程碑

> **本文是 Desktop 运行时与里程碑的唯一真源。** 产品优先级看 [04](04-产品形态.md)，实施波次看 [10](10-Electron迁移与内置浏览器实现计划.md)，具体合同看 [spec 012](../.specs/012-electron-shell-migration/) 和 [spec 013](../.specs/013-codex-app-capability-parity/)。

## 当前实现与目标态

| 层 | 当前实现 | 目标态 |
|---|---|---|
| 桌面宿主 | CodexMonitor 衍生 Tauri | Electron main/preload/renderer |
| UI | React/Vite | 复用并迁入 Electron renderer |
| 本地后端 | Tauri Rust App + Rust daemon/shared core | Rust daemon/shared core |
| agent 内核 | 原装 codex app-server | 保持原装 codex app-server |
| Browser | 未实现产品级 in-app browser | Codex 功能对齐的 main-owned `WebContentsView`/session/registry/CDP |
| 模型适配 | Gateway 原型 | 按需独立 sidecar |
| 工作台/OPC | 部分资产和未完成合同 | 暂停，不在目标主链 |

目标态不能用来描述当前发布能力。

## 目标进程拓扑

```text
BlackRain Electron
  ├─ Main
  │   ├─ app/window/update lifecycle
  │   ├─ permission/download/navigation policy
  │   ├─ Browser backend/WebContentsView registry/session/CDP
  │   └─ Rust daemon supervisor
  ├─ Preload
  │   └─ typed allowlisted IPC
  ├─ React Renderer
  │   └─ projects/threads/approvals/diff/terminal/browser UI + bounds/state
  └─ blackrain_daemon
      ├─ shared domain cores
      ├─ original codex app-server
      │   └─ dedicated CODEX_HOME/config.toml
      └─ optional Model Gateway
```

## 唯一内核合同

1. 只有锁定的原装 `codex-rs` / app-server 执行 agent thread。
2. 不引入任何第二 agent runtime；Browser client、MCP 和 code-mode 等工具进程仍由同一个 app-server tool router 管理。
3. Electron、Browser 和 Gateway 只补宿主或适配能力，不拥有 agent loop。
4. thread、事件、审批、停止、恢复和模型路径只有一套真源。
5. 上游能力是否可用必须由当前锁定版本的探针与 E2E 证明。

## Electron 宿主

Main 负责窗口、权限、Browser、系统集成、更新和 daemon 生命周期。Preload 提供最小类型化桥；renderer 无 Node.js 直通。详细安全、失败与迁移合同看 spec 012。

Browser 页面不是 renderer 的例外能力。`WebContentsView` 只由 main 创建、挂载、隐藏、迁移和销毁；renderer 只上报经过 schema 校验的 bounds、visibility、active tab、窗口 generation 和 UI 遮挡状态。App renderer 与页面 WebContents 都保持 Node off，页面不加载 BlackRain preload。

迁移期允许当前 Tauri App 继续作为参照和开发入口，但不接受永久双宿主。每个兼容层必须有删除任务。

## Rust daemon

daemon 是 Electron 与 codex 之间的本地后端：

- 监管 app-server 和可选 Gateway
- 管理专属 `CODEX_HOME`
- 承载项目、文件、Git、终端等 shared domain core
- 统一双向 JSON-RPC、事件、错误、取消、deadline 和退出恢复
- 接收 app-server Browser tool server request，并转发给 main Browser backend

Electron main 不重写这些领域逻辑；renderer 不直接访问 daemon token。目标生产 transport 是 main 启动 daemon 后的 stdio，不保留固定 `127.0.0.1:4732`。daemon stdout 只传协议，日志走 stderr；重启通过 generation 使旧 request/response 失效。

## In-app Browser

Browser 高度对齐本机研究确认的 Codex App 功能与控制面，同时使用 main-owned `WebContentsView` 代替 Codex 当前观察到的 renderer `<webview>`：

```text
app-server Browser tool request
  -> daemon / authenticated local bridge
  -> main BrowserBackend
  -> session registry / sidebar manager
  -> main-owned WebContentsView
  -> page WebContents
  -> Electron API / Chromium CDP
```

- main 在创建、布局、迁移和每次工具调用时校验 owner window、thread、route、view generation 和 profile。
- P0 使用单一 App 专属持久 partition，保持跨 thread/App restart 登录态。
- 隐藏或侧边栏切换时保留同一个 view/WebContents；隐藏活动仍须在 thread 中可观察和停止。
- tab 迁移通过 main 在窗口 `contentView` 之间重新挂载同一 view，不序列化 Cookie 或重建旁路页面。
- main 集中处理导航、弹窗、权限、一次性 download grant、输入注入和 CDP。
- agent/user 控制权显式转换，用户输入可以立即抢占 agent。
- 默认工具是高层 tabs/locator/CUA/snapshot/artifact API；full CDP 只在 Developer mode。
- 网页与 App renderer 权限隔离；Cookie、网页 secret 和下载正文不默认进入 thread 或日志。
- React 侧边栏通过类型化 IPC 同步 bounds；modal、菜单、tooltip、DPI、多屏、焦点和输入法属于发布验收项。

BlackRain 不复制 Codex 的私有 `browser-client.mjs`，也不复制 Windows 未认证 named pipe。P0 通过公开 app-server dynamic tools 与双向 daemon/main RPC 实现相同控制职责；若后续增加自有 Browser client，仍必须落到同一个 Browser backend。

第一项产品验收见 spec 013。

## 配置与 Gateway

- App 是专属 `CODEX_HOME/config.toml` 的唯一写入者。
- 用户 `~/.codex` 不参与。
- provider secret 存入系统凭据存储。
- 原生 Responses provider 直接连接；不兼容时使用独立 Gateway。
- Gateway 只做协议翻译，不处理 Browser 数据或产品状态。

## Windows 制品目标

最终 Windows 安装包包含：

- Electron Desktop 及必要 Chromium 资源
- Rust daemon
- 锁定的 codex Windows x64 二进制
- 可选 Gateway 运行资源
- 专属 `CODEX_HOME` 模板
- License、NOTICE、更新和诊断资源

Windows MVP 已选择 npm + `electron-builder`/NSIS 作为实施方向；签名证书、更新制品源、发布密钥和回滚策略尚未决策并验证。旧 Tauri NSIS 配置不能自动作为 Electron 发布方案。

## 里程碑

| 里程碑 | 范围 | 当前状态 |
|---|---|---|
| P0-0 | Codex 研究基线、目标架构、唯一内核和 specs 对齐 | 已完成文档决策；不代表功能实现 |
| P0-1 | Tauri 能力矩阵、宿主无关 client、Electron 安全空壳 | 未开始 |
| P0-2 | Electron + daemon 双向 RPC + 真实 Codex thread | 未开始 |
| P0-3 | 单 tab Browser：WebContentsView/registry/隐藏运行/tool call | 未开始 |
| P0-4 | Browser 产品化：多 tab、登录、接管、CDP、下载、权限、恢复 | 未开始 |
| P0-5 | 核心项目/thread/审批/diff/终端与系统能力迁移 | 未开始 |
| P0-6 | Windows 安装、更新、回滚、卸载、安全与性能矩阵 | 未开始 |
| P0-7 | 删除 Tauri runtime、adapter、打包和 CI 入口 | 未开始 |
| P1 | 按能力账本继续补齐 Codex App 差距 | 等待 P0 纵向闭环 |
| 暂停 | 工作台、Session Orchestrator、市场、OPC | 不排期 |

## 发布判定

只有同时满足以下条件，才能声称 Electron 版本可交付：

1. 真实 Codex thread、审批、停止和恢复通过。
2. in-app browser 的登录、接管、下载、权限和崩溃恢复通过。
3. 真实 Codex thread 能通过受控 Browser 工具操作用户看到的同一个页面 WebContents，不存在旁路 headless browser。
4. Windows 安装、首启、升级、回滚和卸载通过。
5. renderer/main/daemon/browser 权限审计通过，本地非授权进程不能调用 Browser backend。
6. 性能和资源基线在目标设备上可接受。
7. 对应 `verification.md` 记录具体日期、制品和证据。
