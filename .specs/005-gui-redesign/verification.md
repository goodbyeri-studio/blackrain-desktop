# Verification — GUI 重做

## 验证矩阵

| 日期 | 范围 | 命令/方式 | 结果 | 备注 |
|---|---|---|---|---|
| 2026-06-26 | 前端历史基线(spec 建立时) | `npm run typecheck` | 通过 | 只代表当日快照,不是当前发布基线 |
| 2026-06-26 | 首页对齐(#43 已合入) | 见 PR #43 | 历史通过 | 环/图标/胶囊/模型菜单的当日验证;历史测试数不再作固定门槛 |
| 待跑 | 基础 token 对齐 | `typecheck` + `test` + `lint:ds` | 未跑 | 阶段 1：换 token 不得回归 |
| 待跑 | 逐界面对齐（首页收尾/对话/设置/弹层/侧栏） | 并排截图对比 + 守卫 | 未跑 | 阶段 2 |
| 待跑 | 4 套主题（light/dark/dim/system） | 人工切换核验 | 未跑 | 改 token 必须四套都验 |
| 待跑 | Windows 主题与真实交互 | `npm run tauri:dev:win` 人工核验 | 未跑 | MVP 主验收,需 Win11 实机 |
| 待跑 | 合规扫描（无 OpenAI 专有字节） | 人工审 + 图标来源核对 | 未跑 | 每个新增图标记来源 |

## 首页 diff 清单（2026-06-26 目测，`~` 为估算，落 token 前须 DevTools 核）

> 来源:用户提供的 BR vs Codex 首页截图及展开/hover 细节图。窗口尺寸不同,故固定元素比绝对值、布局元素比比例。本表包含**视觉、结构与交互差异**;导航文案/品牌/模型名等 BlackRain 内容身份不要求与 Codex 相同。

| 区域 | 元素 | BR 现状 | Codex 范本 | 处置 |
|---|---|---|---|---|
| chrome | 后退/前进箭头 | 无 | 有 | 补 |
| chrome | 右上窗口/面板切换 | 无 | 2 个图标 | 补 |
| 侧栏 | 背景 | 玻璃半透 | 实色 ~`#1b1b1b` | **保留玻璃**(皮肤) |
| 侧栏 | 行高 | ~37px | ~29px | 收窄 |
| 侧栏 | 标签字号 | ~15px | ~13.5px | 收窄 |
| 侧栏 | 左 padding | ~24px | ~16px | 收窄 |
| 侧栏 | 列表项图标 | 无 | ~14px/项 | 补 |
| 侧栏 | 底部 | 单行设置 | 设置+帐户+头像 | 对齐(内容 BR 自定) |
| 主区 | 背景 | 赛博壁纸+玻璃 | 纯黑 ~`#0e0e0e` | **保留壁纸/玻璃**(皮肤，有意决策) |
| 标题 | 字号 | ~40px | ~30px | 改小 |
| 标题 | 颜色 | `#fff` | ~`#ededed` | 略降亮 |
| 标题 | ↔composer 间距 | ~80px | ~40px | 收窄 |
| composer | 结构 | 两段分离 | 一体框 | **改一体** |
| composer | 圆角 | ~16px | ~12px | 改小 |
| composer | 宽度 | ~820pt 偏宽 | ~720pt + max-width | 收约束 |
| 控件 | 权限 pill | 中性灰白 | 按档变色(橙=完全访问) | **抄变色** |
| 控件 | 发送键 ↑ | 细箭头几乎无底 | 实心圆+深箭头 | **改实心圆** |
| 控件 | 模型选择器 | 带头像粗体 | 无头像、字小 | 对齐(模型名 BR 自定) |
| 控件 | 项目入口 | 框外胶囊「进入项目工作」 | 框内「Choose project」(未汉化) | 并入框内，**保留汉化**(BR 更完整) |

## Token 表（阶段 1 量化结果落这里）

> 待莓莓提供 Codex 范本后填。结构示例：

| 类别 | Codex 范本值 | BlackRain 现值 | 对齐后值 | 备注 |
|---|---|---|---|---|
| 主背景 (light) | TBD | TBD | TBD | |
| 主背景 (dark) | TBD | TBD | TBD | |
| 正文字号/行高 | TBD | TBD | TBD | |
| 间距刻度 | TBD | TBD | TBD | |
| 圆角 | TBD | TBD | TBD | |
| 边框/分隔色 | TBD | TBD | TBD | |
| 阴影 | TBD | TBD | TBD | |

## 已验证

- 前端 `typecheck` 在 2026-06-26 快照中通过。
- 开发态 GUI 启动的旧记录来自 macOS `dev-client.sh`,只作 post-MVP 历史证据,不能代替 Windows MVP 验收。
- 首页核心元素已对齐 Codex（PR #43）。

## 未验证风险

- **两级模型菜单依赖网关**：现状网关请求侧不读不转 `reasoning_effort`，前端单做 = 假菜单（点了不传到 DeepSeek）。须 spec 001 配套，未验。
- **侧栏项目展开是有状态功能**：要接真实会话数据 + 相对时间 + hover 态，碰 `useThreadsReducer.ts` 热点，回归风险高，未评估。
- **玻璃/噪点皮肤的可读性**：保留玻璃后，文字对比与边缘清晰度未在各界面验收。
- **composer 一体化是结构改动**：非纯 CSS，碰 composer feature，未验回归。
- **42 RPC 已接线 ≠ GUI 已完成**:spec 006 已记录壳层包装,但本 spec 的搜索/侧栏/管理界面仍需逐项实现与 Windows 交互验证;且当前锁定 rust-v0.146.0 / `e363b08c` 的 capability/Windows 重验未完成。
- Windows 全栈视觉/交互未验。
- 4 套主题下批量改 token 的连锁回归未评估。
- 首页 diff 表数值为目测估算，未经 DevTools 核实。

## 失败记录

- 暂无
