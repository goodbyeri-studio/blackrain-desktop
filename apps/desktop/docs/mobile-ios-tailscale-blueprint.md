# Mobile / iOS / Tailscale 历史蓝图

该路线已暂停，不属于 Windows Electron MVP。迁移前的远程 daemon、移动壳和固定 RPC 路径已从生产代码删除，本文不再提供可执行命令。

重新启动移动或远程产品前必须建立独立 living spec，重新定义单一原装 app-server 内核、远程鉴权、传输、设备 ownership、状态同步和 iOS 签名验收。不得从 Git 历史恢复旧 daemon 作为 Desktop fallback。
